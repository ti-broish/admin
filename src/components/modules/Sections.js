import React from 'react';
import { ContentPanel } from './Modules';

export default props => {
    return(
        <ContentPanel>
            <h1>Секции</h1>
            <hr/>
        </ContentPanel>
    );
};