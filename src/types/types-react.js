/**
 * @template T
 * @typedef {import("react").Dispatch<T>} Dispatch
 */

/**
 * @template T
 * @typedef {(props: T) => JSX.Element} FnComponent
 */

/**
 * @template T
 * @typedef {Dispatch<React.SetStateAction<T>>} SetState
 */

/**
 * @typedef {(e: React.ChangeEvent<HTMLInputElement>) => void} ChangeEventHandler
 */

/**
 * @typedef {import('react').MutableRefObject<HTMLDivElement | null>} Ref
 */

/**
 * @typedef {import("react").MouseEventHandler<HTMLButtonElement>} ClickHandler
 */