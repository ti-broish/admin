//@ts-check

/**
 * @typedef {{
 *   isOpen: boolean,
 *   isRejectionModal?: boolean,
 *   title?: string,
 *   message?: string,
 *   warningMessage?: any,
 *   confirmButtonName?: string,
 *   cancelButtonName?: string,
 *   confirmHandler?: Function,
 *   cancelHandler?: Function,
 *   messageHandler?: Function,
 * }} ModalState
 */

/**
 * @typedef {{
 *   id: number,
 *   isFeatured: boolean,
 *   color: string,
 *   displayName: string,
 * }} Party
 */

/**
 * @typedef {{
 *    value: string,
 *    isValid: boolean,
 *    isTouched: boolean,
 * }} ControlledInput
 */
